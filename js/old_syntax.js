//! Пример засиси через конструктор

"use strict";
/*
function User(fname, sname, age) {
  this.fname = fname;
  this.sname = sname;
  this.age = age;
  User.count++;
}

User.count = 0; //! статическое свойство
User.createTestUser = () => new User("Anonim", "Anonim", "20");

function UserPrototype() {
  this.getFullname = () => `${this.fname} ${this.sname}`;
}

User.prototype = new UserPrototype();

const userOld = new User("Edik", "Tiutiunnyk", 26);
*/
